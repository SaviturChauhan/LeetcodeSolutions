class Solution {
public:
    using ll = long long;

    bool possible(vector<int>& time, ll t, int totalTrips) {
        ll trips = 0;

        for (int x : time) {
            trips += t / x;
            if (trips >= totalTrips) return true;
        }

        return false;
    }