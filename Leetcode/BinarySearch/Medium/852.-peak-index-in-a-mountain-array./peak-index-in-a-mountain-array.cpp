if(arr[mid] > arr[mid-1]){
               pivot = mid;
               l = mid+1;
            int mid = l + (r-l)/2;
        while(l <= r){
            }else{
                r = mid-1;
        int r = n-1;
        int l = 1;
        int pivot = -1;
    int peakIndexInMountainArray(vector<int>& arr) {
        int n = arr.size();
public:
class Solution {
            }
        }
        return pivot;
    }
};